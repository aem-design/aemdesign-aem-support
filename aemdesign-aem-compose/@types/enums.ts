export enum CarouselType {
  LIST = 'component.list',
}

export enum IconPrefix {
  BRANDS = 'fab',
  SOLID = 'fas',
}

export enum LogLevel {
  LOG     = 'log',
  INFO    = 'info',
  WARNING = 'warn',
  ERROR   = 'error',
}
