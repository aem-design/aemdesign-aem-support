import * as config from './lib/config'
import * as loaders from './loaders'
import * as plugins from './plugins'

const modules = { config, loaders, plugins }

export { config, loaders, plugins }

export default modules
